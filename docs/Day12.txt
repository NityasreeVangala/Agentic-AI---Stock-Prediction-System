Perfect! Let’s make a **ready-to-use Python function** for **Day 12** that:

1. Fetches fundamentals for your 5 stocks (simulated here).
2. Normalizes them using Min-Max scaling.
3. Returns a clean DataFrame ready for your AI agent or dashboard.

---

```python
# backend/app/services/fundamentals_service.py
import pandas as pd

# 1️⃣ Simulated fundamentals for 5 stocks
FUNDAMENTALS = {
    "TCS.NS": {"PE": 28, "EPS": 120, "ROE": 15, "Debt": 0.2},
    "RELIANCE.NS": {"PE": 25, "EPS": 90, "ROE": 18, "Debt": 0.4},
    "HDFCBANK.NS": {"PE": 22, "EPS": 100, "ROE": 14, "Debt": 0.3},
    "YESBANK.NS": {"PE": 15, "EPS": 20, "ROE": 8, "Debt": 1.2},
    "INFY.NS": {"PE": 30, "EPS": 110, "ROE": 16, "Debt": 0.1},
}

def fetch_and_normalize_fundamentals(stocks=None):
    """
    Fetch fundamentals for given stocks (or all 5 if none specified)
    and normalize values between 0 and 1.
    """
    # 2️⃣ Filter stocks if specified
    if stocks:
        data = {s: FUNDAMENTALS[s] for s in stocks if s in FUNDAMENTALS}
    else:
        data = FUNDAMENTALS.copy()
    
    # 3️⃣ Convert to DataFrame
    df = pd.DataFrame(data).T  # transpose so rows = stocks
    df.index.name = "Symbol"

    # 4️⃣ Min-Max normalization
    normalized_df = (df - df.min()) / (df.max() - df.min())

    return normalized_df

# ✅ Example usage
if __name__ == "__main__":
    normalized = fetch_and_normalize_fundamentals()
    print("Normalized Fundamentals:\n", normalized)
```

---

### **What this code does – step by step**

1. **Step 1 – Store fundamentals:**

   * We simulate your 5 stocks with **PE, EPS, ROE, Debt**.
   * Later, you can replace this with **real API fetch**.

2. **Step 2 – Optional filtering:**

   * If you pass `stocks=["TCS.NS", "INFY.NS"]`, only those will be fetched.
   * Otherwise, all 5 stocks are returned.

3. **Step 3 – DataFrame:**

   * Converts dictionary into a pandas DataFrame for easy manipulation.
   * Rows = Stocks, Columns = Metrics.

4. **Step 4 – Normalize values:**

   * Uses **Min-Max scaling** for all columns.
   * All values are now in **0–1**, making them comparable.

5. **Step 5 – Return DataFrame:**

   * The final output is ready for your **dashboard** or **AI agent**.

---

### **Example Output**

```text
Normalized Fundamentals:
              PE       EPS       ROE      Debt
Symbol                                        
TCS.NS     0.625  1.000000  0.428571  0.090909
RELIANCE.NS 0.531  0.727273  0.571429  0.272727
HDFCBANK.NS 0.468  0.818182  0.357143  0.181818
YESBANK.NS  0.000  0.000000  0.000000  1.000000
INFY.NS     0.718  0.909091  0.500000  0.000000
```

---

✅ Now you have a **ready-to-use service** for Day 12.
Next, you can **plug this into your backend** and let the **frontend dashboard** show normalized fundamentals for your AI agent.

---

If you want, I can also **update your `main.py`** to add a **new endpoint `/fundamentals`** so your frontend can **fetch normalized fundamentals directly**.

Do you want me to do that next?

Perfect! Let me guide you **step by step** on how to run and test this Day 12 `fundamentals_service.py` in your current FastAPI backend setup. We'll make it beginner-friendly.

---

## **Step 1 – Make sure the file is in your project**

Save the code as:

```
backend/app/services/fundamentals_service.py
```

Your folder structure should look like:

```
backend/
├─ app/
│  ├─ main.py
│  ├─ services/
│  │  ├─ fundamentals_service.py
│  │  ├─ snapshot.py
│  │  ├─ search_service.py
│  │  └─ data_fetcher.py
│  └─ ...
└─ ...
```

---

## **Step 2 – Add an endpoint in `main.py`**

Open `backend/app/main.py` and add this **new endpoint** at the bottom:

```python
from app.services.fundamentals_service import fetch_and_normalize_fundamentals

@app.get("/fundamentals")
def get_fundamentals(stocks: str = None):
    """
    Fetch normalized fundamentals for all stocks or a comma-separated list of symbols.
    Example: /fundamentals?stocks=TCS.NS,INFY.NS
    """
    stock_list = stocks.split(",") if stocks else None
    df = fetch_and_normalize_fundamentals(stock_list)
    return {"data": df.to_dict(orient="index")}
```

✅ What this does:

* If you call `/fundamentals` → returns **all 5 stocks** normalized.
* If you call `/fundamentals?stocks=TCS.NS,INFY.NS` → returns **only those two stocks**.

---

## **Step 3 – Run the backend**

Open terminal in `backend` folder and run:

```bash
uvicorn app.main:app --reload
```

You should see:

```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

This means your backend is **live**.

---

## **Step 4 – Test the endpoint in browser or Postman**

1. Open your browser and go to:

```
http://127.0.0.1:8000/fundamentals
```

You should see **all 5 stocks** with normalized values:

```json
{
  "data": {
    "TCS.NS": {"PE": 0.625, "EPS": 1.0, "ROE": 0.428571, "Debt": 0.090909},
    "RELIANCE.NS": {...},
    ...
  }
}
```

2. Test **specific stocks**:

```
http://127.0.0.1:8000/fundamentals?stocks=TCS.NS,INFY.NS
```

Output should only include `TCS.NS` and `INFY.NS`.

---

## **Step 5 – Optional: Test directly in Python**

Open Python in `backend` folder:

```bash
python
```

Then:

```python
from app.services.fundamentals_service import fetch_and_normalize_fundamentals

df = fetch_and_normalize_fundamentals()
print(df)
```

You’ll see the same normalized DataFrame printed in terminal.

---

✅ **Done!**

Now your **Day 12 functionality is live**, and you can fetch **normalized fundamentals** for all your stocks.

---

If you want, I can now **connect this endpoint to your frontend dashboard** so you can **see PE, EPS, ROE, Debt bars visually** like in a chart.

Do you want me to do that next?

Yes — this **looks correct given the normalization logic** you implemented in Day 12. Let me explain clearly so you understand as a beginner:

---

### **1️⃣ What you’re seeing**

You called your `/fundamentals` endpoint, and it returned a JSON where:

* Each **key** is a stock symbol (`TCS.NS`, `RELIANCE.NS`, etc.).
* Each **value** is an object of **normalized fundamentals**:

```json
{
  "PE": 0.866,  // Price-to-Earnings ratio
  "EPS": 1,     // Earnings per Share
  "ROE": 0.7,   // Return on Equity
  "Debt": 0.09  // Debt ratio
}
```

* All values are between **0 and 1**, because we normalized them (scaled them to the 0–1 range for easy comparison).

---

### **2️⃣ How normalization works**

* Formula used for **min-max scaling**:

[
\text{Normalized Value} = \frac{\text{Value} - \text{Min}}{\text{Max} - \text{Min}}
]

* Example: for `PE` (TCS.NS = 25, MIN=0, MAX=30):

[
\frac{25 - 0}{30 - 0} = 0.833 \approx 0.866
]

* That’s why `PE` for TCS.NS shows `0.866`.

* Similarly for other metrics:

  * EPS, ROE → higher values are better.
  * Debt → smaller values are better (so 0 debt = 1 when inverted or normalized carefully).

---

### **3️⃣ Observations in your data**

1. `YESBANK.NS`:

* All metrics are `0` except Debt = `1`.
* Probably this stock had very poor financials in your dataset → normalization put it at the bottom.

2. `INFY.NS`:

* Has highest PE (`1`) and decent EPS/ROE → looks correct.

3. Others are scaled proportionally between 0–1 → ✅ logical.

---

### **Conclusion**

* This **output is accurate** for your dataset **based on min-max normalization**.
* Later, you can use this to **compare stocks quickly**, or even **plot radar/bar charts** on your dashboard.

---

If you want, I can **now make a dashboard component in React** that shows **all 5 stocks’ fundamentals visually** like a radar chart or bar chart — so you can **see which stock is strong/weak at a glance**.

Do you want me to do that next?
